// Main QML entry. Loaded by main.py.
// Keep presentation here; bind to Python-exposed view-models.

import QtQuick
import QtQuick.Controls
import QtQuick.Layouts

ApplicationWindow {
    width: 800
    height: 600
    visible: true
    title: qsTr("ArchHub")
}
